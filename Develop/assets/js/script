var body = document.body;

var timeContainer = document.getElementsByClassName("container");
var timeBlock = document.getElementById("time-block");
var row = document.getElementById("row");
var hour = document.getElementById("hour");
var textArea = document.getElementById()
var saveBtn = document.getElementById("saveBtn");
var workDay = workHours.length - 1;

hour.textContent = workHours[i];

var workHours = ["9AM", "10AM", "11AM", "12PM", "1PM", "2PM", "3PM", "4PM", "5PM"];
// var timeSpots = document.createElement("ul");
// var editSlot = document.createElement("span");
// var hourSlot = document.createElement("div");
// for (i = 0; i < hours.length - 1; i++) {
//     hourSlot.textContent = hours[i];
// }
// var saveButton = document.createElement("button");

// var timeSlot = hourSlot[i].appendChild(editSlot).appendChild(saveButton);

// timeBlocks.append(timeSpots);
// for (i = 0; i < hours.length - 1; i++) {
//     timeSpots.appendChild(timeSlot.length - 1);
// }

var currentDay = document.querySelector("#currentDay");
var currentDate = moment();
currentDay.textContent = currentDate.format("dddd, MMMM Do");
